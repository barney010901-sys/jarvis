[pytest]
# "." for `app`, ".." (repo root) for `agent` — see docs/DECISIONS.md
# ("Backend imports the agent package via PYTHONPATH, not packaging").
pythonpath = . ..
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
